# charta

**charta** は、技術・研究スライド／論文に載せる図を作成するための、単ページのローカル作図ツールです。
画像を取り込み、その上に矢印・図形・テキスト・数式で注釈を付け、各オブジェクトのプロパティを後から編集し、
出版品質のベクター形式（SVG/PDF）で書き出せることを目指しています。

詳細な設計方針は `CLAUDE.md` を参照してください。

## セットアップと起動

```bash
uv sync
uv run python main.py
```

## テスト

```bash
uv run pytest
```
